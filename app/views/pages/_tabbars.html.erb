<div id="accordion1" role="tablist">
  <div class="card card-collapse">
    <div class="card-header" role="tab" id="headingOne">
      <h5 class="mb-0">
        <a data-toggle="collapse" href="#collapseProducts" aria-expanded="false" aria-controls="collapseProducts">
          <i class='material-icons'>images</i> <%= t('pages.blocks.tabbars.title') %>
          <i class="material-icons">keyboard_arrow_down</i>
        </a>
      </h5>
    </div>

    <div id="collapseProducts" class="collapse show" role="tabpanel" aria-labelledby="headingOne" data-parent="#accordion1">
      <div class="card-body">
        <%= form.fields_for :tabbars do |tabbar| %>
          <%= render 'tabbar_fields', f: tabbar%>
        <% end %>

        <div class="links add-new-icone">
          <%= link_to_add_association raw("<i class='material-icons'>add_circle</i><span>#{t('pages.blocks.tabbars.add')}</span>"), form, :tabbars %>
        </div>
      </div>
    </div>
  </div>
</div>
